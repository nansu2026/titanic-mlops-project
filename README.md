# titanic-mlops-project
This project demonstrates an end-to-end MLOps pipeline using the Titanic dataset.

## Use Case

The model predicts whether a Titanic passenger survived based on passenger details.

## Models Used

- Logistic Regression
- Random Forest

The best model is automatically selected and saved as:

```text
models/best_model.pkl