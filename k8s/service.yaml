apiVersion: v1
kind: Service
metadata:
  name: titanic-mlops-service
  labels:
    app: titanic-mlops-app
spec:
  type: NodePort
  selector:
    app: titanic-mlops-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080